# Historical Scope Audit

Audit date: 2026-09-07. This document records evidence and unresolved interpretation; it does not waive requirements or replace `full-goal.md`. The previous PRD's first-version exclusions are not user authorization to remove capabilities from full delivery.

## H01: Geometry And Modifiers

Original PRD 4.1, lines 97-98:

> 后续增强：挤出、布尔、曲线、吸附增强及可复用场景模板。
> 进一步扩展：更深入的网格编辑、修改器或外部生成式 3D 服务。

The active full goal, section 3.2, explicitly requires mesh editing, extrusion, Boolean operations, curves, terrain and reusable templates. Matrix H01 additionally names an editable operation stack. Existing vertex/face commands, CSG and parametric curve/terrain editing addressed geometric authoring but destructively replaced geometry; history alone did not satisfy editable modifier settings.

The new general modifier stack retains a parametric or mesh source and ordered mirror, array and Loop subdivision settings. Users and MCP can add, edit, disable, reorder, remove and bake modifiers. Existing mesh, curve and terrain controls edit the retained source. Evaluated geometry is shared by viewport rendering, exported frames, collision geometry and continuity checks. Mirror copies have corrected winding but are not welded; intersecting copies are not automatically fused. Boolean modeling of a stack target requires an explicit bake. Complexity limits reject excessive results atomically. This is not a claim of every modifier available in a general DCC package.

Implementation: `shared/modifier-schema.ts`, `modifier-evaluation.ts`, `modifier-operations.ts`, `modeling.ts`, `modeling-operations.ts`; UI `src/components/ModifierPanel.tsx` within object geometry controls. The shared command registry exposes `modifier_add`, `modifier_set`, `modifier_remove`, `modifier_reorder`, `modifier_bake` over MCP. Subdivision uses `three-subdivide` 1.1.5. Templates are implemented separately in `shared/templates.ts` and the SQLite template services/library.

Verification: `tests/modifiers.test.ts` covers order, source edits, actual subdivided geometry, winding/volume, bypass/removal, bake, serialization, curve/terrain sources, limits, locks and rollback. It passes together with the existing modeling regression suite (15 checks). `tests/modifier-service.test.ts` passes actual MCP discovery/editing, idempotent replay, SQLite reopening, undo/redo and invalid-edit rollback. `tests/modifiers.spec.ts` passes real UI/MCP shared editing, source-stack ordering, desktop/mobile controls, moving canvas, bake/undo, reload and a real MCP rendered image (8.7 seconds total). Screenshots were inspected in `.data/modifier-test-results/modifiers-modifier-stack-U-907f7-es-mobile-controls-and-undo/`. Final full-suite acceptance remains the root delivery gate.

## H07: Former Exclusions

Original PRD section 7, line 274:

> 首版不纳入完整雕刻、复杂拓扑编辑、专业材质节点、布料毛发、流体、复杂动力学和高级面部动画。完整产品可按真实创作需求逐步增加能力。

The active goal section 2 explicitly says every deferred/enhanced/unsupported item must be reviewed and cannot be excluded by default. The following table separates what is actually implemented from what remains undefined or absent. It does not turn the old first-version exclusion into a full-product exclusion.

| Historical item             | Current evidence                                                                                                                                                                                                                                                     | Relationship to active requirements and unresolved scope                                                                                                                                                                                                                                                    |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Complete sculpting          | `terrain.sculpt` has raise, flatten and smoothing brushes over editable height fields; indexed mesh vertices are editable. There is no arbitrary-surface sculpt brush, dynamic topology or multiresolution sculpt system.                                            | Terrain authoring is explicitly required and implemented. General sculpting is not equivalent to the terrain brush. Whether full surface sculpting is required by the broad full-product direction remains unresolved; do not mark it implemented or excluded.                                              |
| Complex topology editing    | Indexed vertex/face editing, extrusion, CSG, curves, terrain and a non-destructive modifier stack exist.                                                                                                                                                             | These directly meet named mesh/Boolean/curve requirements. Retopology, edge-loop tools, UV editing and every professional topology workflow have not been implemented and are not individually enumerated in the active goal. Do not silently equate the current set with all possible topology tools.      |
| Professional material nodes | Neutral materials, tones, lighting, shadows and optical focus are implemented. There is no node-based material graph. Imported meshes are rendered using the product's white-model material.                                                                         | White-model rendering is explicitly required. A general professional material graph is neither a prerequisite for neutral rendering nor explicitly named in section 3. Its appearance in the old exclusion still requires a scope decision; the current renderer is not evidence that material nodes exist. |
| Cloth                       | Imported rigs/animation and authored mesh proxies can depict garments; no cloth solver or cloth authoring interface exists.                                                                                                                                          | No cloth simulation appears in the four required film specifications. Mesh or rig animation must not be described as cloth simulation. The old deferred mention remains unresolved.                                                                                                                         |
| Hair                        | Imported geometry can carry a hair silhouette; no strand/groom system or hair solver exists.                                                                                                                                                                         | A white-model silhouette is possible, but this does not implement hair authoring or simulation. The old deferred mention remains unresolved.                                                                                                                                                                |
| Fluids                      | No fluid solver, fluid surface authoring or volumetric fluid system exists.                                                                                                                                                                                          | The required space-film projectile/hit/explosion proxies are explicit white-model effects, not fluid simulation. No named film requires fluids; the old deferred mention remains unresolved.                                                                                                                |
| Complex dynamics            | Rapier provides deterministic rigid-body helpers, collision, gravity/zero-gravity, kinematic bodies, baked editable tracks and transformed geometry checks. Actor IK/contact constraints, coordinated paths, vehicle/flight poses and effect tracks are implemented. | These directly address the explicit requirement for necessary physical helpers and the four productions. They do not implement all soft-body, cloth, fluid or coupled dynamics systems. The broader phrase needs a concrete acceptance definition before it can be accepted.                                |
| Advanced facial animation   | H02/H03 work adds editable facial controls and mature audio-to-viseme analysis; detailed evidence belongs to those rows.                                                                                                                                             | A neutral actor face can carry rendered expressions and visemes. This does not imply photoreal facial solving, a facial capture system or arbitrary imported face compatibility. Audit H02/H03 implementation and tests separately.                                                                         |

A scope decision should address the actual absent capabilities above. No exclusion, completion status or reduced requirement is inferred here from development cost, the first-version label, or absence from a demo.

## H08: External Generation And Chat Entry

Original PRD 4.1, line 98, uses the alternative phrase “修改器或外部生成式 3D 服务”. The following paragraph explicitly defines AI modeling as AI calling tools to create/edit actual 3D scenes and calls arbitrary-text generation of complex people or fine models an additional capability. PRD section 5, line 239, states:

> MCP 是接入协议。外部 AI 的自然语言理解、任务规划和视觉判断由所接入的 Agent 提供；内置聊天助手是可单独增加的产品入口。

PRD section 6, line 252, states:

> AI 状态区域：连接状态、当前操作和任务结果；聊天入口按后续范围决定。

| Item                                     | Current evidence                                                                                                                                                                | Scope judgment                                                                                                                                                                                                                                                              |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| External AI can operate real 3D software | Official MCP server exposes shared project, modeling, animation, camera, time, inspection and render operations. Production command journals record actual use.                 | Explicitly mandatory. This is not optional and must pass UI/MCP sharing, images and real-video acceptance.                                                                                                                                                                  |
| External generative 3D provider          | No provider client, generation job, provider key setting or returned-asset workflow is implemented. GLB/glTF import and procedural modeling do not imply text-to-3D generation. | The source PRD presents this as an alternative/additional integration. The active goal does not name a provider. Keep the absent integration and its conditional source wording recorded pending scope determination; do not call MCP a generative 3D service.              |
| Built-in chat assistant                  | The application provides MCP connection configuration and operation feedback. It has no internal LLM conversation/provider interface.                                           | The original text explicitly leaves this entry to a later scope decision and assigns understanding/planning to the external Agent. The required external-AI workflow exists independently, but H08 must retain the distinction and cannot claim built-in chat is delivered. |

The product explicitly stops at white-model video. None of the above authorizes integration with downstream AI video-generation platforms.

## H09: Lighting Plans

Original PRD section 4.6, line 186 explicitly defers lighting plans alongside visible depth of field and rack focus. Previous H01-H08 rows omitted that named capability. Basic scene lighting and a template that happens to carry lighting settings are not independent plan authoring.

This item is implemented: named editable project lighting plans, scene defaults, independent shot overrides, shared UI/MCP commands, referenced-plan validation, indirect lock protection, template remapping and shared preview/export resolution. The plan uses the established white-model key/ambient lighting parameters; this does not claim professional material nodes or a general DCC light graph. `lighting-plans.test.ts`, `lighting-service.test.ts` and `lighting-plans.spec.ts` provide domain, SQLite, actual UI/MCP and PNG/MP4 agreement evidence. The final frozen-version gate remains recorded in the delivery matrix.
